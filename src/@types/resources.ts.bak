import common from 'public/locales/en/common.json';
import model from 'public/locales/en/model.json';
import db from 'public/locales/en/db.json';

const resources = {
  common,
  model,
  db,
} as const;

export default resources;